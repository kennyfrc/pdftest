<!-- this is linked to the comment tha tyou have created within the comments controller -->
<%= form_for([@post, @post.comments.build]) do |f| %>

  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
